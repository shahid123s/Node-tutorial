const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is : ${num1} + ${num2} `);
}
addValues();


// if your not exported explicitily it will wrap with and IIFE funtion when importing this module to another module ot wrap like IIFE